# ── 运行时/缓存 ──
__pycache__/
*.pyc
*.pyo
*.log
logs/

# ── AI 客户端运行时（角色存档/世界/Mods 编译产物/配置——含个人数据）──
mod/tModLoader/

# ── C# 编译中间产物 ──
*.tmod
mod/NekoTerrariaLink/bin/
mod/NekoTerrariaLink/obj/

# ── 插件运行时数据：用户配置不上传（默认模板 default_config.json 上传）──
data/config/user_config.json
data/config/settings.json
data/bond.json

# ── 密钥/环境 ──
.env
*.key
